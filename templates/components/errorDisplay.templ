package components

templ ErrorDisplay(errors []string){
    <div class="error-display" id="error-display">
        <ul>
            for _, error := range errors{
                <li>{error}</li>
            }
        </ul>
    </div>
}
